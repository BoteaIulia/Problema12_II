using System;

public static class Globals
{
	internal static int Main()
	{
		int n;
		int a;
		int b;
		("%d", n) = Convert.ToInt32(value: Console.ReadLine());
		int x = 0;
		for (int i = 1; i < n; i++)
		{
			scanf_s("%d", a);
			if (a == 0 && b != 0)
			{
				x++;
			}
			b = a;
		}
		return x;
	}
}
